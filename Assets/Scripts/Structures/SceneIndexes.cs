public enum SceneIndexes
{
    // These values should line up with the indices of the Build Settings.
    ROOM_LOADER = 0,
    ROOM_INSTANCE = 1,
    // SPLASH_SCREEN = 0,
    // TITLE_SCREEN = 1,
    // HUB = 2,
}
